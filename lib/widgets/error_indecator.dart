import 'package:flutter/material.dart';

class ErrorIndecator extends StatelessWidget {
  const ErrorIndecator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Somethine went wrong!'),
    );
  }
}
